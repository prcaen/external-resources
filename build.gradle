buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:2.0.0'
  }
}

allprojects {
  repositories {
    jcenter()
  }
}

ext {
  minSdkVersion = 9
  targetSdkVersion = 23
  compileSdkVersion = 23
  buildToolsVersion = '23.0.3'
}